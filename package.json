{
  "name": "pods-finance-subgraph",
  "version": "1.0.0",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ developerfred/pods-finance",
    "create-local": "graph create --node http://localhost:8020/ developerfred/pods-finance",
    "remove-local": "graph remove --node http://localhost:8020/ developerfred/pods-finance",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 developerfred/pods-finance"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.19.0",
    "@graphprotocol/graph-ts": "^0.19.0",
    "node-forge": "~1.3.0",
    "yargs-parser": "^20.2.4"
  },
  "license": "MIT"
}
